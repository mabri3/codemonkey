# cycle26 cost telemetry live probe
# exec --cost-summary --ephemeral vs home server -> stderr summary printed
# (turns/tokens/wall/tool calls), ledger appended to ~/.codemonkey/cost.json
# tests: test_exec_cost_summary_flag asserts "tokens:" and "turns:" in stderr
# result: PASS
