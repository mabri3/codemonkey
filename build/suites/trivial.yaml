name: trivial
tasks:
  - id: t1
    prompt: "Reply with exactly the word pong1 and nothing else."
    expect_stdout_contains: ["pong1"]
    expect_exit: 0
    ephemeral: true
  - id: t2
    prompt: "Reply with exactly the word pong2 and nothing else."
    expect_stdout_contains: ["pong2"]
    expect_exit: 0
    ephemeral: true
  - id: t3
    prompt: "Reply with exactly the word pong3 and nothing else."
    expect_stdout_contains: ["pong3"]
    expect_exit: 0
    ephemeral: true
  - id: t4
    prompt: "Reply with exactly the word pong4 and nothing else."
    expect_stdout_contains: ["pong4"]
    expect_exit: 0
    ephemeral: true
  - id: t5
    prompt: "Reply with exactly the word pong5 and nothing else."
    expect_stdout_contains: ["pong5"]
    expect_exit: 0
    ephemeral: true
  - id: t6
    prompt: "Reply with exactly the word pong6 and nothing else."
    expect_stdout_contains: ["pong6"]
    expect_exit: 0
    ephemeral: true
