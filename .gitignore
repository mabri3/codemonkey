# Secrets — never commit
.env
*.pem
*.key

# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/
.pytest_cache/

# Runtime state (user config dir lives at ~/.codemonkey/, not here)
.codemonkey/

# loop38 cycle 80: branch worktrees live here, never committed
.branches/

# OS
.DS_Store
build/.tick.lock
.codemonkey/
